# 功能模块说明

系统现已按“公共核心 + 独立功能模块”组织。公共核心只提供登录、数据库连接、通用增删改查、附件、Excel、二维码和通用表单能力；业务字段与专用规则放在各自模块中。

## 目录

| 功能 | 后端字段与规则 | 前端专用交互 |
|---|---|---|
| 院校基础库 | `features/schools/` | `web/features/schools.js` |
| 人才库 | `features/candidates/` | `web/features/candidates.js` |
| 应聘登记 | `features/applications/` | `web/features/applications.js` |
| 面试管理 | `features/interviews/` | `web/features/interviews.js` |
| 新员工登记 | `features/employees/` | `web/features/employees.js` |
| 常见问题 | `features/questions/` | `web/features/questions.js` |
| 简历收集与识别 | `features/resume_documents/` | `web/features/resume_documents.js` |

每个后端功能目录包含：

- `manifest.json`：模块名称、菜单顺序和依赖关系；
- `schema.json`：字段、标题、选项和附件配置；
- `service.py`：该功能独有的业务规则；
- `README.md`：模块入口提示。

## 修改一个功能

调整字段或下拉选项时修改该模块的 `schema.json`；调整资料带入、校验等后端规则时修改 `service.py`；调整页面专用交互时修改对应的 `web/features/*.js`。通用表格和表单仍由 `web/app.js` 提供。

数据库已经建成后，增加、删除或改名数据库字段必须同时编写迁移，不能只修改 JSON。

## 安全停用或删除功能

推荐先“停用”，不要直接删除数据库表。编辑 `features/config.json`，把模块名加入 `disabled`：

```json
{
  "disabled": ["questions"]
}
```

重启服务后，该功能不会出现在菜单、统计或通用 API 中，相关公开入口也会返回“该功能已停用”；原数据表和数据继续保留，移出 `disabled` 后即可恢复。

如需永久删除代码和数据，应先查看目标模块 `manifest.json` 的依赖关系，停用并验证，再备份数据库，最后编写数据库迁移删除数据表。人才库是招聘流程的核心依赖，删除人才库前必须同时处理应聘、面试、入职、问答和简历模块。

当前主要依赖链为：院校 → 人才 → 应聘 → 面试 → 入职；简历、问答依赖人才。这里的箭头表示右侧功能会读取左侧功能的数据。停用只隐藏功能，不会破坏这些内部数据关联。

## 公共核心

- `server.py`：HTTP、鉴权、通用 CRUD、数据库迁移与跨模块编排；
- `web/app.js`：页面框架、通用表格、通用表单和附件能力；
- `web/table-tools.js`：批量删除、Excel 和二维码；
- `excel_io.py`：Excel 导入导出。
