# Task 4 review package (no Git repository)

Review `web/corporate-theme.css` from comment `/* QR cards retain` through end against Task 4 brief and report. Earlier rules are approved dependency context.

Pay specific attention to QR image protection, responsive cascade order, focus visibility, reduced motion, and whether the claimed public-page visual inspection used the required routes. The authenticated QR check is explicitly deferred to Task 5.
